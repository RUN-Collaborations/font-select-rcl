/**
 A js file to ensure creation of the ./src/fonts directory before the following build script is run:
    "build": "babel src --out-dir build && shx cp ./src/fonts/*.json ./build/fonts",
*/
"use strict";